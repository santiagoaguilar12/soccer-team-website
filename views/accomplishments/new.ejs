<% include ../partials/header %>
<div class="ui container">
    <form class="ui form" method="POST" action="/players/:id/accomplishments/">
        <h2>Add an Accomplishment</h2>

        <div class="fields">
            <div class="field">
                <label> Accomplishment Title</label>
                <input type="text" name="accomplishment[name_acc]" placeholder="Title">
            </div>
            <div class="field">
                <label> Accomplishment Year</label>
                <input type="number" name="accomplishment[text_acc]" min="1900" max="2099" step="1" placeholder="Year" />
            </div>
        </div>
        <div class="fields">
            <div class="field">
                <label>Accomplishment Description</label>
                <textarea name="accomplishment[text_acc]" placeholder="(Max 750 characters)" rows="4"></textarea>
                
            </div>
            
        </div>
                <button class="ui button" type="submit">Submit</button>

    </form>
</div>

<% include ../partials/footer %>
